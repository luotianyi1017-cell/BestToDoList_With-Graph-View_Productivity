# Fonts Folder

Place local open-source `.ttf` files here.

Current expected fonts:

- `PatrickHand-Regular.ttf` (primary English handwriting style)
- `Noto_Sans_SC/NotoSansSC-VariableFont_wght.ttf` (fallback)

No network font download is used.
